data:
  dataset: cifar10
  batch_size: 64

diffusion:
  num_timesteps: 1000
  beta_start: 1e-4
  beta_end: 0.02

model:
  image_channels: 3
  image_size: 32
  num_timesteps: 1000

training:
  epochs: 200
  learning_rate: 1e-3
  device: auto
